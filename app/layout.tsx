import type {Metadata} from"next";import{CartProvider}from"@/components/cart";import"./globals.css";export const metadata:Metadata={title:"NIMA COLLECTION — Little things. Big energy.",description:"A curated mini-store for NIMA COLLECTION."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body><CartProvider>{children}</CartProvider></body></html>}