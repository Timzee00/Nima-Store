import{NextResponse}from"next/server";import{z}from"zod";import{getAdminSession}from"@/lib/auth";import{sql}from"@/lib/db";import{getAdminProducts}from"@/lib/store";const schema=z.object({name:z.string().min(2).max(120),category:z.string().min(2).max(80),description:z.string().min(5).max(2000),price:z.coerce.number().nonnegative(),salePrice:z.coerce.number().nonnegative().optional().or(z.literal("")),stock:z.coerce.number().int().min(0).max(100000),featured:z.boolean().default(false),image:z.string().url().optional().or(z.literal(""))});function slugify(x:string){return x.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")}export async function GET(){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await getAdminProducts())}export async function POST(req:Request){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});try{const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Check the product fields."},{status:400});const x=p.data,slug=slugify(x.name)+"-"+Date.now().toString(36),images=x.image?[x.image]:["https://placehold.co/900x1125/F0ECE5/171513?text=NIMA"];const rows=await sql.query("INSERT INTO products(name,slug,category,description,price,sale_price,stock,images,featured,active) VALUES($1,$2,$3,$4,$5,$6,$7,$8::jsonb,$9,true) RETURNING id,name,slug,category,description,price,sale_price,stock,images,featured,active",[x.name,slug,x.category,x.description,x.price,x.salePrice===""?null:x.salePrice??null,x.stock,JSON.stringify(images),x.featured]);return NextResponse.json(rows[0])}catch{return NextResponse.json({error:"Could not create product."},{status:500})}}export async function DELETE(req:Request){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});const id=new URL(req.url).searchParams.get("id");if(!id)return NextResponse.json({error:"Missing id"},{status:400});await sql.query("UPDATE products SET active=false,updated_at=now() WHERE id=$1",[id]);return NextResponse.json({ok:true})}