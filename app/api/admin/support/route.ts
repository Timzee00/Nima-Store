import{NextResponse}from"next/server";import{z}from"zod";import{getAdminSession}from"@/lib/auth";import{sql}from"@/lib/db";
const patchSchema=z.object({id:z.string().uuid(),status:z.enum(["open","in_progress","resolved","closed"]).optional(),priority:z.enum(["low","normal","high","urgent"]).optional(),staffNote:z.string().max(2000).optional()});
export async function GET(){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});const rows=await sql.query("SELECT id,ticket_number,order_id,customer_name,phone,category,message,status,priority,staff_note,created_at,updated_at FROM support_tickets ORDER BY created_at DESC LIMIT 100",[]);return NextResponse.json({tickets:rows});}
export async function PATCH(req:Request){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});const p=patchSchema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid support update."},{status:400});const x=p.data;const rows=await sql.query("UPDATE support_tickets SET status=COALESCE($1,status),priority=COALESCE($2,priority),staff_note=COALESCE($3,staff_note),updated_at=now() WHERE id=$4 RETURNING id,ticket_number,status,priority,staff_note,updated_at",[x.status??null,x.priority??null,x.staffNote??null,x.id]);if(!rows[0])return NextResponse.json({error:"Ticket not found."},{status:404});return NextResponse.json(rows[0]);}