import{NextResponse}from"next/server";import{z}from"zod";import{getAdminSession}from"@/lib/auth";import{sql}from"@/lib/db";import{getRecentOrders}from"@/lib/store";const schema=z.object({id:z.string(),status:z.enum(["pending","confirmed","fulfilled","cancelled"])});export async function GET(){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});return NextResponse.json(await getRecentOrders())}export async function PATCH(req:Request){if(!await getAdminSession())return NextResponse.json({error:"Unauthorized"},{status:401});const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid status"},{status:400});await sql.query("UPDATE orders SET status=$1,updated_at=now() WHERE id=$2",[p.data.status,p.data.id]);return NextResponse.json({ok:true})}