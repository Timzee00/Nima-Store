import{NextResponse}from"next/server";import{z}from"zod";import{createAdminSession,getAdminEmail,verifyAdminPassword}from"@/lib/auth";const schema=z.object({email:z.string().email(),password:z.string().min(8).max(200)});export async function POST(req:Request){try{const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Enter a valid email and password."},{status:400});const email=getAdminEmail(),ok=p.data.email.toLowerCase()===email.toLowerCase()&&await verifyAdminPassword(p.data.password);if(!ok)return NextResponse.json({error:"Invalid admin credentials."},{status:401});await createAdminSession(email);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Unable to sign in."},{status:500})}}