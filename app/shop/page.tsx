export const dynamic="force-dynamic";import{StoreHeader}from"@/components/store-header";import{Storefront}from"@/components/storefront";import{getCategories,getProducts}from"@/lib/store";export default async function Shop(){const[p,c]=await Promise.all([getProducts(),getCategories()]);return <><StoreHeader/><main className="container"><div className="section" style={{paddingBottom:30}}><div className="eyebrow">Nima Collection</div><h1 style={{fontSize:"clamp(52px,9vw,110px)",letterSpacing:"-.07em",lineHeight:.86,margin:"12px 0"}}>The full edit.</h1><p style={{maxWidth:520,color:"var(--muted)",lineHeight:1.6}}>Browse every available piece, filter by category and open any product for the full story.</p></div></main><Storefront products={p} categories={c}/></>}