import{redirect}from"next/navigation";import{getAdminSession}from"@/lib/auth";import{getSupportTickets}from"@/lib/store";import{AdminSupport}from"@/components/admin-support";export default async function SupportAdmin(){if(!await getAdminSession())redirect("/admin/login");const tickets=await getSupportTickets();return <main className="admin-shell"><div className="admin-top"><div><strong>NIMA.</strong><span style={{opacity:.45,marginLeft:10}}>Support desk</span></div><a className="btn secondary" style={{borderColor:"rgba(255,255,255,.2)",color:"#fff"}} href="/admin">Back to dashboard</a></div><main className="admin-main"><div style={{marginBottom:24}}><div className="eyebrow">Customer care</div><h1 style={{fontSize:"clamp(38px,5vw,62px)",letterSpacing:"-.06em",margin:"5px 0 0"}}>Support tickets.</h1></div><AdminSupport initial={tickets}/></main></main>}