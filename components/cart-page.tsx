"use client";import Link from"next/link";import{Minus,Plus,X}from"lucide-react";import{useCart}from"./cart";export function CartPage(){const{items,subtotal,change,remove}=useCart();return <main className="container"><section className="section"><div className="eyebrow">Shopping bag</div><h1 style={{fontSize:"clamp(48px,8vw,100px)",letterSpacing:"-.07em",lineHeight:.86}}>Your bag.</h1>{!items.length?<div className="card" style={{padding:35,marginTop:30}}><p>Your bag is empty.</p><Link className="btn" href="/shop">Continue shopping</Link></div>:<div className="bag-layout"><div className="card bag-items-card">{items.map(i=><div className="bag-line" key={i.productId}><div><strong>{i.name}</strong><div style={{color:"var(--muted)",marginTop:5}}>₦{i.price.toLocaleString()}</div><div className="qty"><button onClick={()=>change(i.productId,-1)} aria-label={"Decrease quantity of "+i.name}><Minus size={13}/></button><span>{i.quantity}</span><button onClick={()=>change(i.productId,1)} aria-label={"Increase quantity of "+i.name}><Plus size={13}/></button></div></div><button className="icon-btn" onClick={()=>remove(i.productId)} aria-label={"Remove "+i.name}><X size={15}/></button></div>)}</div><aside className="card bag-summary"><div className="eyebrow">Summary</div><div className="cart-total" style={{marginTop:15}}><span>Subtotal</span><span>₦{subtotal.toLocaleString()}</span></div><Link className="btn" href="/checkout" style={{width:"100%"}}>Continue to checkout</Link><Link className="btn secondary" href="/shop" style={{width:"100%",marginTop:8}}>Continue shopping</Link></aside></div>}</section></main>}