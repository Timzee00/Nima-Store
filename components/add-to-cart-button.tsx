"use client";import{ShoppingBag}from"lucide-react";import{Product}from"@/lib/store";import{useCart}from"./cart";export function AddToCartButton({product,disabled}:{product:Product;disabled?:boolean}){const{add}=useCart(),price=Number(product.salePrice??product.price),image=product.images[0]||"https://placehold.co/900x1125/F0ECE5/171513?text=NIMA";return <button className="btn" disabled={disabled} onClick={()=>add({productId:product.id,name:product.name,price,image})}><ShoppingBag size={17}/>{disabled?"Out of stock":"Add to bag"}</button>}