"use client";import{Search,Sparkles}from"lucide-react";import{useMemo,useState}from"react";import{Product}from"@/lib/store";import{ProductCard}from"./product-card";export function Storefront({products,categories}:{products:Product[];categories:string[]}){const[category,setCategory]=useState("All"),[search,setSearch]=useState("");const filtered=useMemo(()=>{const q=search.toLowerCase().trim();return products.filter(p=>(category==="All"||p.category===category)&&(!q||(p.name+" "+p.description).toLowerCase().includes(q)))},[products,category,search]);return <section id="shop" className="section"><div className="container"><div className="section-head"><div><div className="eyebrow">The edit</div><h2>Find your next favourite.</h2></div><div className="searchbar"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search the collection"/></div></div><div className="category-row" style={{marginBottom:24}}>{["All",...categories].map(x=><button key={x} className={"pill "+(category===x?"active":"")} onClick={()=>setCategory(x)}>{x}</button>)}</div>{filtered.length?<div className="product-grid">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="card" style={{padding:48,textAlign:"center"}}><Sparkles size={28} style={{margin:"0 auto 12px"}}/><strong>No pieces match that search.</strong><p style={{color:"var(--muted)"}}>Try another word or switch categories.</p></div>}</div></section>}