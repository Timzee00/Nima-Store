"use client";import{ArrowDown}from"lucide-react";import{useEffect,useRef}from"react";
export function ScrollHero(){const sectionRef=useRef<HTMLElement>(null),videoRef=useRef<HTMLVideoElement>(null);useEffect(()=>{let raf=0;const update=()=>{const s=sectionRef.current,v=videoRef.current;if(!s||!v||!v.duration)return;const total=s.offsetHeight-window.innerHeight;const p=Math.max(0,Math.min(1,(window.scrollY-s.offsetTop)/Math.max(total,1)));v.currentTime=p*v.duration};const onScroll=()=>{cancelAnimationFrame(raf);raf=requestAnimationFrame(update)};window.addEventListener("scroll",onScroll,{passive:true});window.addEventListener("resize",onScroll);vload();function vload(){videoRef.current?.addEventListener("loadedmetadata",update)}return()=>{cancelAnimationFrame(raf);window.removeEventListener("scroll",onScroll);window.removeEventListener("resize",onScroll)}},[]);
return <section ref={sectionRef} className="hero" style={{height:"145vh"}}><div className="hero-stage"><video ref={videoRef} className="hero-video" src="/hero.mp4" poster="/hero-poster.svg" muted playsInline preload="metadata"/><div className="hero-shade"/><div className="hero-content"><div className="hero-kicker">Nima Collection</div><h1>Little things. Big energy.</h1><p className="hero-copy">A curated mix of everyday accessories, statement pieces and useful little upgrades — chosen to make your space and your style feel more like you.</p><div style={{display:"flex",gap:10,marginTop:24,flexWrap:"wrap"}}><a className="btn" href="#shop">Shop the collection</a><a className="btn secondary" style={{borderColor:"rgba(255,255,255,.35)",color:"#fff"}} href="#story">Our point of view</a></div><div className="hero-scroll"><ArrowDown size={15}/> Scroll to explore</div></div></div></section>}