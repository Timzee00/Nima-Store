"use client";import Link from"next/link";import{Search,ShoppingBag}from"lucide-react";import{useCart}from"./cart";export function StoreHeader(){const{count,open}=useCart();return <header className="site-header"><div className="container nav"><Link className="brand" href="/">NIMA.</Link><nav className="nav-links"><a href="#shop">Shop</a><a href="#story">About</a><a href="#footer">Contact</a></nav><div className="nav-actions"><a className="icon-btn" href="#shop" aria-label="Browse"><Search size={18}/></a><button className="icon-btn" onClick={open} aria-label="Open cart"><ShoppingBag size={18}/>{count>0&&<span className="cart-count">{count}</span>}</button></div></div></header>}